//for - executar várias vezes um mesmo comando, declaração ou bloco de código
//iterar um objeto ou um array, passar vez por vez

//for(inicializador; condição; incremento ){
//declaraçãpo dentro do for, que é o bloco de código que vai rodar todas as vezes que o for executar}

//inicializador = usado para inicializar o contador de variáveis, pode criar no próprio for usando let ou fora do for e iterar.

//condição = expressão que será avaliada e se o resutdado for true a declaração que vem dentro do for vai ser executada.

//incremento = executar com +1 - i++

for (let i = 0; i < 155; i++){
    console.log(i)
    console.log("Olá Mundo")

}
