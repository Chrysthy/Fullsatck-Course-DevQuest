// Tipos de variáveis
// São a estruturas fundamental do código
// 6 tipos: string, number, boolean, null, undefined e simbol

//string: um dado tipo texto e pode ser escrito com "" ou ''
let nome = "Chrystine"

//number: é número - decimais, inteiros, negativos e casas decimai
let numero = 12

//boolean: vrificar se a condição é verdadeira ou falsa
let usuarioVerificado = true

if (usuarioVerificado) {
    console.log("Usuário Verificado")
}
else {
    console.log("Não foi possível verificar o usuário")
}

//null: nulo, não será criado nada na memória, inválida, não exite ou que é inválida
let pessoa = null

//undefined não consegue definir o tipo da variável, indefinido
let idade = undefined
console.log(idade)